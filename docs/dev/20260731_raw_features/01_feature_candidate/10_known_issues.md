# 10. 알려진 결함과 처리 순서

- 작성일: 2026-08-15
- 발견 경위: `06_grade_a_deep_dive/08`~`11`(T2 4개 family 상세 조사)을 쓰면서
  feature mart를 직접 조회하다 나온 것들. 공식 run이 보고한 내용이 아니라
  **원본 값을 열어봐야 보이는 것들**이다.
- 대상: Phase B `20260812T231507-f9117ce1` + 결합 AB `20260813T130307-f9117ce1`
  (snapshot `2026-08-12`, config `e55c3046…`)
- 이 문서는 **무엇이 잘못됐는지와 어떤 순서로 고칠지**만 다룬다. 각 family의
  통계 해석은 `06_grade_a_deep_dive/` 아래 파일에 있다.

> **전제.** 피쳐 정의가 바뀌면 이번 라운드 등급은 그대로 두고 새 config로 다시
> 검정한다. 사후에 정의를 고쳐 등급을 바꾸면 사전등록 규율이 무너진다. 그래서
> 아래 순서는 "고칠 수 있는 것부터"가 아니라 **"재검정을 유발하지 않는 것부터"**로
> 짜여 있다.

## 1. 한눈에

| ID | 문제 | 위치 | 심각도 | 현재 산출물 영향 | 재검정 유발 |
|---|---|---|:---:|:---:|:---:|
| I1 | 윈저라이즈가 결측을 1분위 값으로 대체 | `research/etl/features/fin_scan.py:216-231` | 높음 | 있음 (19.3% 행) | **예** |
| I2 | 같은 패턴이 라벨 코드에도, 주석은 반대로 서술 | `research/etl/labels.py:199-205` | 중간 | 없음 | 아니오 |
| I3 | 감소 사유 카탈로그 문자열 불일치 | `research/etl/features/event_scan.py:84` | 높음 | 있음 (음수 0건) | **예** |
| I4 | 0 편중으로 분위 분석이 성립하지 않음 | issuance · payout | 중간 | 있음 | 아니오(해석) |
| I5 | 극단값·음수 잔존 | `ev_payout_yield` | 중간 | 있음 | 조건부 |
| I6 | 정정 비율이 등급 상한을 결정 | payout · value_z | 낮음 | 등급 B 고정 | **예**(재수집 시) |
| I7 | 표본 길이·매핑 대체율 | `fin_value_z` | 낮음 | 검정력 제약 | **예**(보강 시) |
| I8 | 표 머리말과 실제 기준 불일치 | `09_all_feature_results.md` §1 | 낮음 | — | 아니오 |
| I9 | 피쳐 코드 변경을 식별할 fingerprint 부재 | `horizon_scan_phase_b.py:237-256` | 중간 | 재현성 | 아니오 |
| I10 | 마트 캐시가 스키마만 봐서 산식 변경을 놓친다 | `research/etl/mart.py:60-73` | 중간 | 재현성 | 아니오 |

## 2. I1 — 결측이 "가장 비싼 쪽"으로 대체된다

**증상.** `fin_value_z`가 있는 405만 행 중 **78.3만 행(19.3%)**이 네 구성요소가
전부 NULL인데도 값을 갖는다. 그 값은 그날 그 시장의 최솟값이다.

**원인.** 윈저라이즈 구문이다.

```sql
LEAST(GREATEST(fin_book_to_market, p01), p99) AS w_bm
```

DuckDB의 `GREATEST`/`LEAST`는 NULL을 건너뛴다.

```
LEAST(GREATEST(NULL, 0.1), 0.9)  →  0.1     -- NULL이 아니다
```

그래서 값이 없는 구성요소가 **1분위 값으로 조용히 채워진다.** 결과가 둘이다.

- `value_component_count`가 4로 세어져 **"2개 이상 유효할 때만 값을 낸다"는 규칙이
  무력화된다.**
- 대체값이 하필 하한이라 결측 종목이 전부 "가장 비싼" 끝단에 몰린다.
  2025-05-02 기준 KOSPI 942종목 중 194개, KOSDAQ 1,731종목 중 107개가 각 시장
  바닥값에 붙어 있다.

**신호 자체는 무사하다.** 해당 행을 빼고 decile을 다시 만들면 스프레드가
0.76%p → 0.89%p로 오히려 커진다. 결측이 신호를 만든 게 아니라 **희석하고 있었다.**

**수정.**

```sql
CASE WHEN fin_book_to_market IS NULL THEN NULL
     ELSE LEAST(GREATEST(fin_book_to_market, p01), p99) END
```

**검증.** 구성요소가 NULL인 행에서 `value_component_count`가 줄고, 4개 모두
NULL이면 `fin_value_z`가 NULL이 되는지 단위 테스트로 고정한다.

상세: `06_grade_a_deep_dive/11_fin_value_z.md` §4.

## 3. I2 — 같은 착각이 라벨 코드에도 있다

`research/etl/labels.py:199-205`, `y_reg_{h}d`를 만드는 자리다.

```python
# per-date winsorize via quantile clip (NULL passes through cleanly)
f"LEAST(GREATEST({raw}, QUANTILE_CONT({raw}, {lo}) OVER ({win})), ..."
```

**주석이 사실과 반대다.** NULL은 통과하지 않고 하한값이 된다. 같은 함수의
`y_rank`/`y_cls`는 `CASE WHEN {raw} IS NULL THEN NULL`로 막아뒀는데 `y_reg`만
빠졌다. 바로 위 주석이 그 가드를 넣은 이유를 설명하고 있는데도 그렇다.

**현재 영향은 없다.** 이 SQL이 만드는 `label_daily`는 compute 파이프라인이
materialize하지 않고(`compute_all.py`는 `materialize_label_scan`만 호출한다),
유일한 소비처인 `research/models/_01_20_access_return_rank`는 타깃으로
`y_rank_20d`를 쓴다. `y_reg_*`를 타깃으로 돌리는 순간 드러난다.

테스트도 `y_reg`를 전혀 건드리지 않는다. **주석이 거짓이라 다음 사람이 그대로
믿을 위험**이 실제 위험이다.

## 4. I3 — 감소 사유가 한 번도 매칭되지 않는다

**증상.** `ev_net_share_issuance_yoy` 313만 관측치 중 **음수가 0건**, 최솟값이
정확히 0이다. 이름은 순발행인데 실제로는 총발행을 재고 있다.

**원인.** 카탈로그 문자열과 데이터가 어긋난다.

```python
ECONOMIC_DECREASE_REASONS   = frozenset({"감자(유상)"})
MECHANICAL_DECREASE_REASONS = frozenset({"주식병합", "감자(무상)", "무상감자", "소각"})
```

`dart_capital_change_raw`의 사유는 16종뿐이고, 감소 쪽은 둘밖에 없다.

| 데이터의 문자열 | 건수 | 매칭 |
|---|---:|---|
| `무상감자` | 2,840 | mechanical_decrease ✅ (v2에서 추가) |
| `유상감자` | **699** | **없음 → unclassified** |

`감자(유상)`·`감자(무상)`·`소각`·`주식병합`은 **데이터에 한 건도 없다.** 즉
`ECONOMIC_DECREASE_REASONS`는 죽은 집합이고 `economic_decrease_1y`는 항상 0이라,
`경제적 증가 − 경제적 감소`가 음수가 될 수 없다.

`04_specific_plan_B.md` §4.4.2의 v2 검토가 `무상감자`를 "이미 매핑된 `감자(무상)`과
같은 행위의 다른 표기"로 추가했는데, **정확히 같은 어순 문제인 `유상감자`는
빠졌다.** 그 검토가 annual vintage 이벤트 259건만 봤고 거기에 `유상감자`가 없었기
때문으로 보인다.

**두 번째 영향.** 유상감자는 unclassified로 떨어지고, 산식은 `unclassified_1y = 0`
일 때만 값을 낸다. 유상감자가 한 건이라도 낀 1년 창은 값 자체가 사라진다.
`issuance_classification_complete = false`인 행이 24,012건(전체의 0.36%),
해당 법인 88곳이다.

**수정.** `ECONOMIC_DECREASE_REASONS`에 `유상감자`를 추가하고
`EVENT_FEATURE_FORMULA_VERSION`을 `issuance_v3`으로 올린다. 정확 일치 원칙은
유지한다 — `04_specific_plan_B.md`가 어순 정규화·부분 일치를 금지한 이유가
여전히 유효하다.

**주의.** 자사주 소각을 mechanical로 두는 것은 사전등록된 설계 선택이지 결함이
아니다(`04_specific_plan_B.md` §4.4 2단계). 다만 문헌의 net share
issuance(Daniel·Titman, Pontiff·Woodgate)는 분할조정 주식수 변화를 보므로 매입·소각
에서 음수가 난다. **같은 이름의 다른 지표**다. 어차피 `소각` 문자열도 데이터에
없어 현재로선 무의미한 논점이다.

상세: `06_grade_a_deep_dive/09_ev_net_share_issuance_yoy.md` §4.

## 5. I4 — 0 편중으로 분위 분석이 성립하지 않는다

| family | 정확히 0인 비율 | 공식 분위 스프레드 |
|---|---:|---|
| `ev_net_share_issuance_yoy` | 96.3% | **NULL** (계산 자체가 안 됨) |
| `ev_payout_yield` | 45.3% | 40–60일 셀 **-0.0002** |

`per_date_market_quantile_spread`는 평균순위를 쓴다. 96%가 한 덩어리로 묶이면 그
덩어리의 평균순위가 0.5 근처라 **하위 20% 그룹이 아예 만들어지지 않고**, 행이
통째로 버려져 스프레드가 NULL이 된다.

payout은 계산은 되는데 값이 사실상 0이고 부호도 미세하게 반대다. 같은 셀의 rank
IC가 +0.0523인데도 그렇다. **순위 상관은 있는데 상하위 분위를 사고파는 방식으로는
잡히지 않을 수 있다**는 뜻이라, acceptance gate에서 실제 매매 형태로 확인해야 한다.

코드 결함은 아니다. 다만 **"IC가 유의하다"를 "분위 매매로 수익이 난다"로 읽으면
안 된다**는 것을 등급표만 보고는 알 수 없으므로, 카드에 0 비율을 같이 싣는 편이
좋다.

## 5.5 2단계 조사 결과 (2026-08-15)

I5·I6·I7의 원인을 원본에서 확인했다. **두 가지가 애초 계획을 바꿨다.**

### 5.5.1 I5 — 원천이 같은 항목을 두 단위로 적는다

극단값의 범인은 시총 분모가 아니라 배당 총액이다. 일양약품(007570)이 명확한
사례다.

| 접수 | 사업연도 | `현금배당금총액(백만원)` 값 | 해석 |
|---|---|---:|---|
| 20200330000518 | 2019 | **2,249,158,200** | 원 단위로 적음 |
| (다음 해 보고서) | 2020 | **2,249** | 백만원 단위, 정상 |

같은 금액인데 단위가 다르다. 파이프라인은 행 이름이 `(백만원)`이라는 이유로
무조건 1e6을 곱하므로 1e6배 부풀려진다. 123040도 같은 패턴이다(1,050,574,275원 =
1,051백만원).

**규모.** `thstrm` 배당 총액 17,859행 중 단위 오류는 **4행/4종목**뿐이다.
삼성전자 2020년 20.3조는 특별배당이라 진짜 값이므로, **절댓값 임계로는 거를 수
없다.** 음수는 27행/12종목이다.

→ 수정 방향은 클리핑이 아니라 **단위 검증**이다. 시총 대비 비율이 물리적으로
불가능한 행(예: 배당이 시총의 10배)을 감지해 NULL 처리하고, 그 건수를 품질
진단에 남긴다. 값을 1e6으로 나눠 "고치는" 추정은 하지 않는다.

### 5.5.2 I6 — 재수집으로 해결되지 않는다 (계획 변경)

정정 비율을 metric별로 분해했더니 전부 임계값 0.10 언저리에 몰려 있다.

| metric | 정정 비율 |
|---|---:|
| `net_income` | 0.1056 |
| `operating_cash_flow` | 0.1013 |
| `total_equity` | 0.1012 |
| `total_assets` | 0.1012 |
| `gross_profit` | 0.1002 |
| `controlling_net_income` | 0.0996 |
| `revenue` | 0.0990 |

**약 10%는 이 원천의 기본 정정률이다.** 특정 metric이 유독 더러운 게 아니라,
임계값이 baseline 바로 위에 놓여 있어 metric마다 동전 던지기처럼 A/B가 갈린다.

게다가 **재수집은 이 비율을 낮추지 못한다.** 정정은 실재하는 공시이고, vintage를
더 모으면 보이는 정정이 늘어 비율이 오히려 **올라간다.** 3단계에서 정정분
재수집을 하려던 계획은 근거가 없어져 뺀다.

→ 남는 선택지는 임계값·판정 방식을 다시 정하는 것인데, 이번 라운드 등급을
올리려고 사후에 임계값을 바꾸면 사전등록 규율이 무너진다. **다음 라운드
사전등록 시점에 근거와 함께 결정한다**(§11 4번).

### 5.5.3 I7 — 진짜 병목은 revenue·gross_profit 매핑이다

`fin_value_z` 구성요소의 연도별 non-NULL 비율이다.

| 연도 | B/M | E/P | CFO/P | **S/P** |
|---|---:|---:|---:|---:|
| 2017 | 0.0% | 28.3% | 0.0% | 0.0% |
| 2019 | 10.7% | 51.7% | 0.0% | 0.0% |
| 2021 | 85.9% | 57.0% | 76.3% | **5.1%** |
| 2025 | 75.9% | 51.6% | 70.4% | **4.7%** |

**S/P는 사실상 죽은 구성요소다.** 4개 중 하나로 문서에 적혀 있지만 5% 남짓만
채워진다. vintage 품질표를 보면 이유가 분명하다 — `revenue`는 8,103행,
`gross_profit`은 7,362행뿐인데 `net_income`은 141,011행이다. **매출 계열 canonical
매핑이 거의 비어 있다.**

같은 원인이 `fin_gross_profitability`의 coverage 0.03도 만든다. 즉 I7은 두 family를
동시에 살리는 단일 원인이고, XBRL fallback 보강이 정답이다.

### 5.5.4 I1의 실제 범위는 19.3%가 아니라 29.2%다 (수정)

구성요소 개수별 분포를 다시 셌다.

| 실제 non-NULL 구성요소 수 | 행 수 | 그중 값이 있는 행 |
|---:|---:|---:|
| 0개 | 2,838,777 | **783,183** |
| 1개 | 963,708 | **400,031** |
| 2개 이상 | 2,866,951 | 2,866,951 |

"2개 이상일 때만 값을 낸다"는 규칙을 어긴 행이 783,183 + 400,031 = **1,183,214행**,
현재 `fin_value_z` 값 4,050,165개의 **29.2%**다. 앞서 19.3%로 적은 것은 4개 모두
NULL인 경우만 센 것이었다.

**수정하면 coverage가 0.607 → 약 0.43으로 떨어진다.** 유효 시작일도 밀린다 —
2019년은 ≥2 조건을 만족하는 비율이 6.8%뿐이고, 2020년 56.9%, 2021년 이후 79% 수준
이다. 표본이 더 짧아지므로 **I7(매핑 보강)을 같은 라운드에 넣어야 손실을 상쇄한다.**

## 6. I5 — `ev_payout_yield`에 남은 극단값과 음수

- **극단값**: 최댓값 21,603 (시총의 216만 %). 특정 종목의 2016년 11~12월
  관측치들이라 시총 분모 오류가 거의 확실하다. 중앙값 0.0041, 상위 1% 0.1347,
  상위 0.1% 0.5137이므로 0.5를 넘으면 이미 꼬리다.
- **음수**: 9개 종목 1,513행. 자사주 매입 현금이 음수(성호전자 -77.8억)이거나
  현금배당 총액 자체가 음수(코스텍시스 -3.4억)다.

공식 IC는 순위 기반이라 영향이 없다. 문제는 **이 값을 선형 모델에 그대로 넣으면
한 종목이 계수를 지배한다**는 것이다.

**결정이 필요하다.** 윈저라이즈를 피쳐 정의에 넣으면 재검정 대상이고, 모델 쪽
전처리로 두면 이번 라운드 등급은 건드리지 않는다. **후자를 권한다** — 피쳐는
원값 그대로 두고 모델 입력 단계에서 처리하는 편이 다른 피쳐와도 일관된다.
음수는 원인이 원천 파싱이므로 그쪽에서 고치는 게 맞다.

## 7. I6 · I7 — 등급과 검정력을 묶고 있는 원천 품질

| 항목 | 값 | 결과 |
|---|---|---|
| `ev_payout_yield` 정정 비율 | 0.1146 (`treasury_share_acquisition_amount`) | 등급 상한 B |
| `fin_value_z` 정정 비율 | 0.1056 (`net_income`) | 등급 상한 B |
| `fin_value_z` 매핑 대체율 | 0.3557 (`controlling_net_income`) | 매핑 변경에 취약 |
| `fin_value_z` 표본 | 2019-10-30부터 5년 3개월 | 서브기간 3개, cum 0–120 비중첩 검정 불가 |
| `fin_gross_profitability` coverage | 0.0315 | 신호 없음이 아니라 표본 없음 |

두 B등급 모두 **통계가 아니라 원천이 발목을 잡았다.** 정정분을 다시 받고 canonical
매핑을 보강하면 등급이 바뀔 수 있다. 다만 값이 바뀌므로 재검정 대상이다.

## 8. I8 — 문서 하나

`09_all_feature_results.md` §1 표 머리말은 "IC는 |IC|가 가장 큰 primary 셀
기준"이라고 적었는데, 실제로는 **등급이 확정된 셀**을 실었다. 25개 중 3개 행이
어긋난다.

| family | 표에 적힌 셀 | 실제 \|IC\| 최대 셀 |
|---|---|---|
| `ev_payout_yield` | 40–60일 +0.0523 | cum 0–120 **+0.0972** |
| `fin_value_z` | 40–60일 +0.0348 | cum 0–120 **+0.0722** |
| `ev_net_share_issuance_yoy` | 40–60일 -0.0083 | cum 0–120 **-0.0118** |

나머지 22개는 두 기준이 일치한다. 머리말을 "등급이 확정된 셀 기준"으로 고치면
된다. 숫자는 그대로 두는 게 맞다 — 등급 셀을 싣는 쪽이 표의 목적에 맞다.

## 9. I9 — 피쳐 코드가 바뀌어도 fingerprint가 그대로다

`phase_b_run_spec.json`이 기록하는 지문은 셋이다.

| 지문 | 무엇을 덮는가 |
|---|---|
| `config_hash` | `horizon_scan_config.yaml` 전체의 sha256 |
| `phase_b_code_hash` | `research/analysis/horizon_scan_phase_b*.py`만 |
| `event_feature_formula_version` | issuance 카탈로그(`issuance_v2`) **하나뿐** |

즉 **`fin_scan.py`를 고치면(I1) 값은 달라지는데 세 지문이 전부 그대로다.** 같은
지문으로 다른 숫자가 나오는 상태가 되고, `04_specific_plan_B.md` §1.3이 정한
"mapping rule이 다르면 기존 artifact를 재사용하지 않는다"는 규칙이 작동하지
않는다. `git_commit`/`git_dirty`가 run spec에 남아 추적 자체는 되지만, 재사용
판정에는 쓰이지 않는다.

**수정.** `event_feature_formula_version`과 같은 방식으로 재무 피쳐 쪽
fingerprint(`FIN_FEATURE_FORMULA_VERSION`)를 만들어 run spec과 family card에
싣는다. 값을 바꾸지 않는 순수 계측 변경이라 이번 라운드 재검정을 유발하지 않는다.

## 9.5 I10 — 마트 캐시가 산식 변경을 놓친다 (3단계에서 발견)

`research/etl/mart.py`의 캐시 키는 두 개였다.

```python
{"analysis_config_hash": ..., "schema_hash": _schema_hash(con, select_sql)}
```

`_schema_hash`는 `DESCRIBE`의 결과, 즉 **컬럼 이름과 타입만** 해싱한다. I1처럼 NULL
가드를 넣거나 I3처럼 매핑 문자열을 고치면 출력 스키마는 그대로이므로 **캐시가 유효한
것으로 판정되고 낡은 마트가 조용히 재사용된다.**

즉 I9(run spec fingerprint)를 고쳐도 이 층에서 한 번 더 새는 구조였다. 4단계에서
`force=True` 없이 Phase B를 돌렸다면 수정 전 숫자가 "새 run"의 얼굴로 나왔을 것이다.

**수정.** 캐시 metadata에 `sql_hash`(쿼리 텍스트 해시)를 추가했다. 기존 마트의
metadata에는 이 키가 없으므로 **legacy로 인정해 통과**시킨다 — 키 하나 추가하자고
5.2GB 레이크를 전부 다시 만들 이유는 없다. 이 시점 이후의 산식 변경은 잡힌다.

## 9.6 진행 기록 (2026-08-15)

| 단계 | 항목 | 상태 |
|---|---|---|
| 0 | I8 09 문서 표 머리말 정정 | ✅ |
| 0 | I2 `labels.py` `y_reg` NULL 가드 + 주석 정정 + 테스트 2개 | ✅ |
| 1 | I9 `FIN_FEATURE_FORMULA_VERSION`·`PAYOUT_FEATURE_FORMULA_VERSION` 도입, run spec·card·report 배선 | ✅ |
| 2 | I5·I6·I7 원인 조사 | ✅ (§5.5) |
| 3 | I1 `fin_scan` 결측 처리 → `fin_v2` | ✅ |
| 3 | I3 `유상감자` 매핑 → `issuance_v3` | ✅ |
| 3 | I5 배당 단위 검증·음수 배제 → `payout_v2` | ✅ (2차 보강 포함) |
| 3 | I10 마트 캐시에 `sql_hash` 추가 | ✅ |
| 3 | 사전등록 문서 `04_specific_plan_B.md` §4.4.3·§4.5.1·§4.1 갱신 | ✅ |
| 3 | 골든/회귀 테스트 7개 (전부 수정 전 실패 확인) | ✅ |
| 3 | I7 `revenue`/`gross_profit` 매핑 보강 | ⏸ 분리 |
| 3 | 문헌형 순발행 별도 family 등록 | ⏸ 분리 |
| 4 | feature mart 재빌드 | 진행 중 |
| 4 | Phase B → AB 재실행 | 대기 |

**Phase A는 재실행하지 않는다.** 세 수정이 전부 코드 변경이라 `horizon_scan_config.yaml`이
그대로이고, 따라서 `config_hash`도 그대로다. `load_phase_a_primary_rows`의 config_hash
검사를 기존 Phase A run(`20260813T081646-00fa0e76`)이 그대로 통과한다. 약 4시간 40분을
아꼈다.

**분리한 두 건의 이유.**

- **I7 매핑 보강** — 원인은 확인했다(§5.5.3). XBRL fact에 `ifrs_Revenue` 184,846건,
  `ifrs_GrossProfit` 178,829건이 있는데 카탈로그가 `ifrs-full_` 접두사만 매핑한다.
  I3과 같은 종류의 표기 변형 누락이다. 다만 수정 대상이
  `src/krx_collector/definitions/`(수집기 canonical 층)이고 `xbrl_sourced_rows`가 전
  연도 0이라는 별도 문제가 겹쳐 있어, 연구 마트 수정과 성격이 다르다.
- **문헌형 순발행 family** — config YAML 변경이라 `config_hash`가 바뀌고 **Phase A
  재실행(약 4시간 40분)을 유발한다.** I7과 함께 다음 라운드로 묶는 편이 비용이 낮다.

## 9.7 4단계 1차 검증 결과 (마트 재빌드)

두 마트를 재빌드해(11분 45초) 수정이 실제로 먹었는지 확인했다.

### 9.7.1 I1 — 예측대로다

| 지표 | 수정 전 | 수정 후 |
|---|---:|---:|
| `fin_value_z` 값 개수 | 4,050,165 | **2,866,937** |
| "2개 이상" 규칙 위반 | 1,183,214 | **0** |
| `value_component_count` 분포 | 4가 3,330,799 (허수 포함) | 2/3/4 = 1,024,312 / 1,695,722 / 146,903 |

§5.5.4에서 예측한 2,866,951과 14행 차이다. 규칙 위반은 완전히 사라졌다.

### 9.7.2 I3 — 음수가 생겼다

| 지표 | 수정 전 | 수정 후 |
|---|---:|---:|
| `ev_net_share_issuance_yoy` 값 개수 | 3,130,059 | **3,441,582** |
| 음수 값 | **0** | **585** (최솟값 -0.5259) |

유상감자가 economic_decrease로 잡히면서 음수 통로가 열렸고, unclassified로 버려지던
창이 살아나 값이 311,523개 늘었다.

### 9.7.3 I5 — 1차 수정이 두 경로를 놓쳤다

재빌드 후에도 최댓값 18,339, 음수 953행이 남았다. 원인을 다시 봤다.

- **DPS가 없는 단위 오류**: 344860은 `현금배당금총액` 358,740,000을 적었는데 그 공시에
  DPS 행이 없다. 비교 대상이 없으니 1차 검증이 발동하지 않는다.
- **음수 자사주 현금**: 남은 음수 953행은 전부 `buyback_cash_ttm < 0`이었다. 1차
  수정은 배당 쪽만 막았다.

두 경로를 추가로 막았다. 자사주 취득금액이 음수면 그 성분을 NULL로 떨어뜨리고(배당
쪽은 살린다), 그래도 남는 물리적으로 불가능한 값은 `PAYOUT_YIELD_MAX = 10`(시총의
1000%)으로 걸러낸다. 관측 분포가 중앙값 0.4%, 99% 13.5%, 99.9% 51%라 진짜 꼬리와는
세 자릿수 떨어져 있다.

### 9.7.4 I11 — 공식 run이 쓴 마트가 현재 입력으로 재현되지 않는다

**이번 작업에서 가장 무거운 발견이다.**

`issuance_classification_complete`가 true → false로 51,138행 뒤집히는, 카탈로그
추가만으로는 설명되지 않는 변화가 보였다. **카탈로그만 v2로 되돌려** 현재
레이크에서 다시 만들고 비교했다.

| | 값 개수 | 음수 | unclassified |
|---|---:|---:|---:|
| 공식 run이 쓴 마트(보존본) | 3,130,059 | 0 | 24,012 |
| **v2로 지금 재빌드** | **3,440,600** | 0 | 75,988 |
| v3(카탈로그 수정본) | 3,441,582 | **585** | 74,701 |

**v2로 다시 만든 결과가 공식 run이 쓴 마트와 다르다**(플래그 기준 52,228행).
코드도 정책도 같다. 반면 v2 → v3 차이는 값 +982개, 음수 585개로 유상감자
699행·88개 법인 규모와 정확히 맞는다. 즉 51,138행 flip은 내 수정이 만든 것이
아니다.

> **검증 상태.** 남은 가능성은 둘이다 — (a) 공식 run이 낡은 마트를 재사용했다,
> (b) 마트 빌드가 비결정적이다. (b)를 배제하려면 같은 코드로 두 번 빌드해
> 비교해야 하는데, 첫 시도는 스크립트가 뷰 등록 오류로 실패해 **같은 파일을 자기
> 자신과 비교한 무효한 테스트**였다. 아래 (a) 서술은 그 확인이 끝나기 전까지
> 잠정이다.

기전은 I10이다. 마트 캐시 키가 (config hash, 출력 스키마)뿐이라, 그 사이
`dart_capital_change_raw`가 71,535 → 245,120행으로 늘어도(strict PIT 재수집, `09`
§8) 캐시는 유효로 판정된다. Phase B는 `register_phase_b_marts(con, lake)`를
`force=False`로 부르므로 **낡은 마트를 그대로 재사용**했다. raw export 자체는
23:14에 245,120행으로 완료됐고 Phase B는 23:15에 시작했으니, 데이터는 있었는데
마트가 갱신되지 않은 것이다.

**의미.** A등급을 받은 `ev_net_share_issuance_yoy`의 공식 숫자는 raw가 뒷받침하는
것보다 약 31만 개 적은 관측치 위에서 계산됐다. 방향이 뒤집힐 종류의 문제는
아니지만(§9.7.2), **기록된 입력으로 재현되지 않는다**는 점에서 그 라운드의 재현성
주장은 성립하지 않는다. 4단계 재실행으로 대체되는 것이 맞다.

`fin_value_z` 쪽도 같은 확인이 필요하다 — 다만 `feat_fin_scan_daily`는
`stock_metric_vintage_fact`에만 의존하고 그 마트는 이번에 변경되지 않았다.

## 10. 작업 순서 제안

재검정 비용이 순서를 지배한다. **YAML을 한 글자라도 바꾸면 `config_hash`가 바뀌고,
`load_phase_a_primary_rows`가 Phase A run의 `config_hash` 불일치를 예외로
막는다**(`horizon_scan_phase_b_run.py:1024-1030`). 즉 T2만 손대도 AB 결합을 위해
**Phase A까지 다시 돌려야 한다.** 그래서 정의 변경은 모아서 한 번에 한다.

### 0단계 — 지금 바로 (재검정 없음)

1. **I8** 09 문서 머리말 수정. 5분.
2. **I2** `labels.py` `y_reg` NULL 가드 추가 + 주석 정정 + 단위 테스트.
   산출물에 영향이 없어 되돌릴 위험이 없다.

이 둘은 다른 어떤 결정도 기다릴 필요가 없다.

### 1단계 — 계측 먼저 (재검정 없음)

3. **I9** `FIN_FEATURE_FORMULA_VERSION` 도입, run spec·family card에 기록.

**이걸 I1보다 먼저 해야 한다.** 순서가 뒤바뀌면 수정 전후 artifact를 지문으로
구분할 수 없다. 값이 안 바뀌므로 지금 라운드 결과는 그대로 유효하다.

### 2단계 — 조사 (재검정 없음)

4. **I5** 극단값 종목의 시총 분모가 왜 틀렸는지, 음수 배당·자사주 현금이 파싱
   문제인지 원천 문제인지 확인.
5. **I6** 정정분 재수집 범위 산정 — `treasury_share_acquisition_amount`와
   `net_income`의 정정이 어느 연도·어느 보고서에 몰려 있는지.
6. **I7** `controlling_net_income`·`gross_profit` 매핑 대체 경로를 XBRL fallback으로
   메울 수 있는지, 메우면 `fin_value_z` 유효 시작일이 얼마나 앞당겨지는지.

조사만 하고 코드는 건드리지 않는다. **3단계에서 무엇을 한 묶음으로 바꿀지 정하기
위한 단계**다. 산출물은 짧은 메모 하나면 된다.

### 3단계 — 정의 변경 일괄 (여기서 재검정이 발생한다)

2단계 결과로 범위가 바뀌었다. **정정분 재수집(I6)은 빠지고**(§5.5.2), 매핑
보강(I7)의 대상이 `revenue`/`gross_profit`으로 좁혀졌다(§5.5.3).

7. **I1** `fin_scan.py` 결측 처리 수정 (`fin_v2`).
8. **I3** `유상감자` 매핑 추가 (`issuance_v3`).
9. **I5** 배당 총액 단위 검증 + 음수 배제 (`payout_v2`).
10. **I7** `revenue`/`gross_profit` canonical 매핑 보강 — I1이 깎을 표본을
    상쇄한다. 범위가 커지면 이것만 다음 라운드로 분리해도 된다.
11. `04_specific_plan_B.md`에 변경 근거를 v3 절로 추가하고 config
    `schema_version`을 올린다. **결과를 보기 전에 문서를 먼저 고정한다.**
12. 골든 테스트 — NULL 입력 → NULL 출력(I1), `유상감자` → `economic_decrease`(I3),
    단위 오류 행 → NULL(I5).

**한 묶음으로 가는 이유.** 하나씩 고치면 그때마다 Phase A+B+AB 전체를 다시
돌려야 한다. 묶으면 한 번이다.

### 4단계 — 재실행과 문서 갱신

12. 새 `config_hash`로 Phase A → Phase B → AB 순서로 재실행.
13. `06_grade_a_deep_dive/08`~`11`, `09_all_feature_results.md` 숫자 갱신.
    **이전 라운드 문서는 지우지 않는다** — 등급이 어떻게 바뀌었는지가 그 자체로
    기록이다.
14. 이 문서의 해당 항목에 처리 결과를 적는다.

### 하지 않는 것

- **이번 라운드 등급을 사후 수정하지 않는다.** I1·I3을 고치면 `fin_value_z`와
  `ev_net_share_issuance_yoy`의 숫자가 바뀌지만, 그건 새 config의 결과이지 지금
  등급의 정정이 아니다.
- **acceptance gate를 3단계 전에 돌리지 않는다.** 정의가 바뀔 피쳐를 채택
  후보로 올리면 그 판정을 다시 해야 한다.

## 11. 결정이 필요한 것

1. ~~**I5의 윈저라이즈를 어디에 둘 것인가**~~ → **결정: 둘 다 아니다.** 2단계에서
   원인이 원천 단위 불일치로 확인됐으므로(§5.5.1) 클리핑이 아니라 단위 검증으로
   처리한다. 극단값은 통계적 꼬리가 아니라 데이터 오류다.
2. **문헌형 net share issuance를 별도 family로 사전등록할 것인가** →
   **결정: 한다.** 지금 것은 총발행이고 문헌형은 순발행이라 서로 다른 가설이다.
   같은 이름으로 뭉개는 대신 별도 family로 등록해 비교한다. BH 모집단이 113 → 117
   정도로 늘어나는 비용은 감수한다.
3. ~~**3단계를 언제 시작할 것인가**~~ → **결정: 지금 묶는다.** I6 재수집이 빠져
   준비 시간이 사라졌다(§5.5.2). I1·I3·I5·I7을 한 라운드로 간다.
4. **정정 비율 임계값 0.10을 유지할 것인가** (신규) — §5.5.2가 보여주듯 이 원천의
   기본 정정률이 약 10%라 임계가 baseline에 걸쳐 있다. 지금 라운드에서 바꾸면
   사후 등급 조작이 되므로, **다음 라운드 사전등록 시점에** 근거와 함께 정한다.
   후보는 (a) 임계를 baseline 위로 올린다, (b) 정정 비율을 등급 상한이 아니라
   경고로 격하한다, (c) metric별 상대 비교로 바꾼다.
