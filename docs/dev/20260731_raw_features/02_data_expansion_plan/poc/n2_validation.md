# N2-10 검증 — 기업개황 전량 수집 결과

측정일 2026-08-15 · V6 추가 측정 2026-08-27 · 대상 prod 원천과
`snapshot_date=2026-08-23/source=sj2_remote` 로컬 lake · V1~V5 쿼리
[`n2_verification.sql`](n2_verification.sql)

## 수집 결과

`dart sync-corp-profile --universe-scope historical`, Cronicle 일회성 이벤트 `emsugdoe907`,
단일 run 1,301초.

| | |
|---|---:|
| 요청 | 3,959 |
| 저장 | 3,959 |
| skip | 0 |
| 무응답(`no data`) | 0 |
| 오류 | 0 |

**ticker를 가진 적 있는 법인 3,959건 전부 수집됐다.** 그중 1,330건이 상폐 법인이다.
`--universe-scope historical`이 없었다면 2,629건만 받고 나머지는 생존편향으로 남았다.

## V1 — `induty_code` 결측률

기준 2% 미만. **실측 0.000% (3,959 중 0건).** PoC 150 표본의 결측 0을 전량에서 재확인했다.

자릿수 분포는 PoC대로 섞여 있다. **"N자리로 자른다"가 아니라 prefix여야 한다**는 근거다.

| 자릿수 | 2 | 3 | 4 | 5 |
|---|---:|---:|---:|---:|
| 법인 수 | 44 | 1,387 | 481 | 2,047 |

## V2 — 그룹 크기 분포 — **기준 미달, 규칙을 고쳤다**

기준은 "그룹당 20 이상"이었다. **한 단계 병합으로는 이 기준에 도달하지 못했다.**

| | 병합 전 | 섹션 병합 후 | **2단계 병합 후** |
|---|---:|---:|---:|
| 그룹 수 (ACTIVE 2,629) | 60 | 43 | **33** |
| 20 미만 그룹 | 32 | **11** | **0** |
| 최소 그룹 크기 | 1 | **2** | **20** |

섹션으로 접어도 `A`=4, `S`=2, `E`=3처럼 **섹션 자체가 20 미만**이라 접을 곳이 없었다.
상장 유니버스가 그 섹션을 거의 채우지 않기 때문이다.

**이게 왜 중요한가.** 이 그룹은 업종 내 z-score를 내려고 존재한다. 구성원이 2명인
z-score는 값이 무엇이든 항상 ±0.707이다. 약한 신호가 아니라 **만들어낸 신호**다.
그런 그룹을 남겨두고 하위 호출자가 알아서 걸러주길 기대하는 게 이 저장소가 반복해서
발견하고 있는 실패 방식이다.

**조치:** 병합을 최소 크기를 만족할 때까지 반복한다 — 2자리 → KSIC 섹션 → `OTHER`.
`MIN_GROUP_SIZE=20`은 그대로다. 사전 등록된 문턱은 유지하고, **그 문턱을 지키지 못하던
메커니즘을 고쳤다.** 결과는 최소 그룹 20(ACTIVE) / 23(전체), 위반 0, `OTHER` 100명.

상위 그룹은 26(전자부품, 293) · 29(기계, 214) · 58(출판, 187) · 21(의약, 181) · 20(화학, 171).
금융 그룹(64·65·66) 소속은 ACTIVE 211 · 전체 476이며, **지주회사가 섞여 있다는 한계는
그대로다**(LG와 KB금융이 둘 다 `64992`).

## V3 — `corp_cls` vs `stock_master.market`

기준 불일치 0. **ACTIVE 종목 기준 실측 0.** `K`↔KOSDAQ 1,797 · `Y`↔KOSPI 832, 예외 없다.

첫 측정에서 31건이 불일치로 잡혔는데, 전부 `corp_cls='E'`(기타)였다. 확인해 보니
**`corp_cls='E'`와 `stock_master.status='DELISTED'`가 정확히 같은 31건**이다 —
양방향 모두 0건 불일치. 그중 최근 10일 내 거래된 건 2건뿐이고, 나머지는 마지막 거래일이
2026-04-10 ~ 2026-08-10에 걸쳐 있다.

**두 가지 결론.**

1. `sync_universe`의 상폐 diff가 제대로 작동하고 있다. **서로 독립인 두 소스가 같은
   31건을 지목한다.** 이건 검증 수단으로 쓸 수 있다 — 두 집합이 벌어지면 둘 중 하나가 틀린 것이다.
2. **`corp_cls`는 현재 상태 필드다.** 과거 어느 날짜의 시장 구분으로 쓸 수 없다.
   시장 라벨은 `stock_master`(그리고 PIT는 N3 스냅샷)에서 와야 한다.

## V4 — `acc_mt != '12'`

PoC 추정 약 158. **실측 142** (3,959 중), 그중 **현재 상장 종목은 53건**.

| `acc_mt` | 12 | 03 | 06 | 09 | 02 | 11 | 10 | 04 | 01 | 08 | 05 | 07 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 법인 | 3,817 | 47 | 41 | 11 | 9 | 8 | 6 | 6 | 5 | 4 | 4 | 1 |

**`metric_vintages.py`의 `period_end` 하드코딩 수정 여부 판단은 시총 비중을 봐야 확정된다.**
53종목이 소형주에 몰려 있으면 영향이 작고, 대형주가 섞이면 크다. `daily_market_cap`이
채워진 뒤(N1-8) 다시 잰다. **지금 단계에서는 "종목 수 53"까지가 확정 사실이다.**

## V5 — `est_dt`

결측 0, 미래 날짜 0. 범위는 1897-02-19 ~ 2025-11-03.

1900년 이전 3건은 **이상치가 아니라 실제 값**이다 — 신한은행(1897-02-19),
동화약품(1897-09-25), 우리은행(1899-01-30). 동화약품은 국내 최고(最古) 기업으로 알려져 있다.
**임계값 1900이 틀렸지 데이터가 틀린 게 아니다.**

설립 10년 버킷은 1990년대 850 · 2000년대 961 · 2010년대 682로 완만하게 분포한다.
**firm age 피쳐는 쓸 수 있다.**

## V6 — 업종별 `fin_value_z` 중앙값 분산

**측정 완료. 업종 효과가 크다는 가설이 확인됐다.** 8월 23일 A0/B mart와 현재 업종 그룹으로
`feat_fin_scan_daily_ind` 7,211,785행을 만들었다. 분석 구간은 Horizon Scan과 같은
2014-06-01 이후다.

| 항목 | 결과 |
|---|---:|
| ticker / 최종 그룹 | 3,959 / 37 |
| 일반 그룹 최소 크기 / `OTHER` | 23 / 100 ticker |
| plain / 업종 중립 `fin_value_z` 유효 행 | 4,068,096 / 4,065,295 |
| 업종 중립에서만 빠진 값 | 2,801 (**plain 대비 0.069%**) |
| 일별 횡단면 rank correlation 중앙값 | **0.8305** (p10 0.7704, p90 0.8859) |
| 값의 절대 변화 중앙값 / p90 / p99 | 0.1750 / 0.6339 / 1.6044 |
| 업종 중앙값 사이 표준편차 중앙값 | **0.2832 → 0.0958** |
| 업종 중앙값 사이 IQR 중앙값 | **0.2584 → 0.1003** |
| 업종 중립화 뒤 between-industry 표준편차 감소 | **65.4%** |

표준편차 비교는 날짜×시장마다 `fin_value_z` 유효 종목이 20개 이상인 업종만 남기고, 그런
업종이 5개 이상인 4,600 date-market에서 계산했다. 결과는 기간을 나눠도 같은 방향이다.

| 기간 | 일별 rank corr 중앙값 | plain → 업종 중립 between-industry std | 감소 |
|---|---:|---:|---:|
| 2014–2018 | 0.8051 | 0.1880 → 0.1053 | 48.4% |
| 2019–2022 | 0.8393 | 0.2352 → 0.0988 | 55.9% |
| 2023–2026 | 0.8368 | 0.3273 → 0.0865 | 73.9% |

plain의 체계적 업종 중앙값도 컸다. 유효 종목 20개 이상인 관측에서 낮은 쪽은 그룹
`70` −0.6564, `21` −0.4016, `59` −0.3498이었고 높은 쪽은 `30` +0.3095,
`47` +0.1918, `64` +0.1528이었다. 즉 plain `fin_value_z`는 종목의 상대 밸류뿐 아니라
업종의 구조적 밸류 수준도 상당히 읽고 있었다.

### V6 한계

- master 전체 그룹은 최소 20을 지키지만, 재무 값이 있는 **날짜별 유효 구성원**은 더 적다.
  214,434개 group-date-market 중 20 미만이 148,847개(69.4%), 10 미만이
  105,214개(49.1%)다. 위 핵심 분산 수치는 `n>=20`만 사용했다.
- 업종이 없는 184 ticker·357,689행은 `fin_value_z` 유효 값이 0건이라 V6 결과에 섞이지 않았다.
- materialize 첫 실행에서 `industry_group`이 `resolved`·`ratios` CTE를 통과하지 못하는 SQL
  배선 누락이 드러났다. 세 CTE와 최종 출력까지 컬럼을 전달하도록 고쳤고 binder 회귀 test를
  추가했다. targeted test는 10개 통과했다.
- 이 그룹은 현재 `company.json` 업종을 과거에 소급한다. 결과는 **진단 근거**일 뿐이며
  `feat_fin_scan_daily_ind`를 scored backtest나 새 Horizon Scan 모집단에 넣지 않는다.
  PIT 업종 경로를 제공하는 N4가 선행돼야 한다.

## PIT 금지선 — 유지

`company.json`은 **현재 업종만** 준다. 이력이 없다. 과거 z-score에 미래 분류를 넣는
구조라 [`03_w1_company_profile.md`](../03_w1_company_profile.md) §6의 제한이 그대로 간다.

- 진단 전용
- scored backtest · acceptance gate · holdout **금지**
- PIT 버전은 N4(지수 편입 이력)를 기다린다

V3에서 `corp_cls`가 현재 상태임이 다시 확인됐으므로 이 금지선은 더 강해졌다.

## 남은 것

- [ ] V4 시총 비중 — N1-8 결과로 별도 집계
- [x] V6 업종별 밸류 분산 — 8월 23일 lake에서 측정
- [x] N2-9 `fin_scan.py` 업종 중립 variant materialize·coverage 확인 (진단 전용)
