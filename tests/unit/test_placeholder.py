"""Unit test placeholders.

Suggested test areas:
    - Domain models: construct Stock, DailyBar, verify fields and immutability.
    - Enums: round-trip Market.KOSPI.value == "KOSPI".
    - Config: Settings validation (computed DSN, defaults).
    - Timezone helpers: now_kst(), today_kst(), to_kst().
    - Trading calendar: get_trading_days with known holidays.
    - Retry decorator: verify backoff timing and exception propagation.
"""


def test_placeholder() -> None:
    """Placeholder — replace with real tests."""
    assert True
