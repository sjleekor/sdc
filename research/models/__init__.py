"""Per-model dataset assembly + training (L2b).

Each model lives under ``research/models/_<NN>_<slug>/`` and composes the
model-agnostic ETL layer (``research.etl``) into a model-specific dataset.
"""
